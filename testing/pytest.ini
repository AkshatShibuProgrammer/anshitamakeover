[pytest]
testpaths = api e2e
addopts = -v --tb=short -p no:cacheprovider
markers =
    public: public (anonymous) endpoints
    admin: admin-portal endpoints (staff only)
    chatbot: AI concierge endpoint
    e2e: Playwright browser end-to-end flows
filterwarnings =
    ignore::DeprecationWarning