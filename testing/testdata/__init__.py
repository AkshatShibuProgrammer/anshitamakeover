"""Canonical regression test data for Anshita Makeover.

``factories.py`` builds the deterministic dataset; ``fixtures/`` holds the
serialised JSON snapshot; ``generate_fixture.py`` rebuilds the snapshot.
"""
